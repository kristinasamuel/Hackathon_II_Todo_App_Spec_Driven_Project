apiVersion: apps.kubernetes.io/v1
kind: Deployment
metadata:
  name: scheduler-service
  namespace: todo-app
  labels:
    app: scheduler-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scheduler-service
  template:
    metadata:
      labels:
        app: scheduler-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "scheduler-app"
        dapr.io/app-port: "8001"
        dapr.io/config: "appconfig" # Refer to a Dapr Configuration if needed
        dapr.io/log-level: "debug"
    spec:
      containers:
        - name: scheduler-service
          image: todo-scheduler # This image needs to be built and pushed to a registry
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8001
          env:
            - name: DAPR_HTTP_PORT
              value: "3500"
            - name: DAPR_GRPC_PORT
              value: "50001"
            - name: DATABASE_URL
              value: "postgresql+psycopg2://postgres:example@postgres:5432/todo_db"
          resources:
            limits:
              cpu: "300m"
              memory: "256Mi"
            requests:
              cpu: "150m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: scheduler-service
  namespace: todo-app
  labels:
    app: scheduler-service
spec:
  selector:
    app: scheduler-service
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP
