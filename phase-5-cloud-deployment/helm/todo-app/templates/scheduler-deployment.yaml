apiVersion: apps.kubernetes.io/v1
kind: Deployment
metadata:
  name: {{ include "todo-app.fullname" . }}-scheduler
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: scheduler-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "todo-app.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: scheduler-service
  template:
    metadata:
      labels:
        {{- include "todo-app.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: scheduler-service
      annotations:
        dapr.io/enabled: {{ .Values.dapr.enabled | quote }}
        dapr.io/app-id: "{{ include "todo-app.fullname" . }}-scheduler-app"
        dapr.io/app-port: {{ .Values.dapr.appPortScheduler | quote }}
        dapr.io/config: {{ .Values.dapr.config | quote }}
        dapr.io/log-level: {{ .Values.dapr.logLevel | quote }}
    spec:
      containers:
        - name: scheduler-service
          image: "{{ .Values.imageScheduler.repository }}:{{ .Values.imageScheduler.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.imageScheduler.pullPolicy }}
          ports:
            - containerPort: {{ .Values.dapr.appPortScheduler }}
          env:
            - name: DAPR_HTTP_PORT
              value: "3500"
            - name: DAPR_GRPC_PORT
              value: "50001"
            - name: DATABASE_URL
              value: "postgresql+psycopg2://{{ .Values.database.user }}:{{ .Values.database.password }}@{{ include "todo-app.fullname" . }}-postgres:{{ .Values.database.port }}/{{ .Values.database.name }}"
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app.fullname" . }}-scheduler-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: scheduler-service
spec:
  selector:
    {{- include "todo-app.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: scheduler-service
  ports:
    - protocol: TCP
      port: {{ .Values.dapr.appPortScheduler }}
      targetPort: {{ .Values.dapr.appPortScheduler }}
  type: ClusterIP
