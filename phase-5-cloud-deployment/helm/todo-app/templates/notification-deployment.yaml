apiVersion: apps.kubernetes.io/v1
kind: Deployment
metadata:
  name: {{ include "todo-app.fullname" . }}-notification
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: notification-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "todo-app.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: notification-service
  template:
    metadata:
      labels:
        {{- include "todo-app.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: notification-service
      annotations:
        dapr.io/enabled: {{ .Values.dapr.enabled | quote }}
        dapr.io/app-id: "{{ include "todo-app.fullname" . }}-notification-app"
        dapr.io/app-port: {{ .Values.dapr.appPortNotification | quote }}
        dapr.io/config: {{ .Values.dapr.config | quote }}
        dapr.io/log-level: {{ .Values.dapr.logLevel | quote }}
    spec:
      containers:
        - name: notification-service
          image: "{{ .Values.imageNotification.repository }}:{{ .Values.imageNotification.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.imageNotification.pullPolicy }}
          ports:
            - containerPort: {{ .Values.dapr.appPortNotification }}
          env:
            - name: DAPR_HTTP_PORT
              value: "3500"
            - name: DAPR_GRPC_PORT
              value: "50001"
            # Notification service might not need direct DB access if it only consumes events
            # But including for consistency if it logs or stores notification history
            - name: DATABASE_URL
              value: "postgresql+psycopg2://{{ .Values.database.user }}:{{ .Values.database.password }}@{{ include "todo-app.fullname" . }}-postgres:{{ .Values.database.port }}/{{ .Values.database.name }}"
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app.fullname" . }}-notification-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: notification-service
spec:
  selector:
    {{- include "todo-app.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: notification-service
  ports:
    - protocol: TCP
      port: {{ .Values.dapr.appPortNotification }}
      targetPort: {{ .Values.dapr.appPortNotification }}
  type: ClusterIP
